{
  "name": "jiangcheng-lights",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "build:edgeone": "vite build --mode edgeone --outDir output/edgeone-release/build",
    "deploy:edgeone": "node tools/edgeone-deploy.mjs deploy",
    "edgeone:preview": "node tools/edgeone-deploy.mjs refresh"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.5",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "vite": "^6.1.0"
  }
}
